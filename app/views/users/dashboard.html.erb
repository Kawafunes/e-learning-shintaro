<div class="All">
    <div class="container">
        <div class="row mt-5">
            <div class="col-4">
                <div class="card" style="width: 18rem;">
                    <div class="card-body">
                        <% if current_user.avatar.url != nil %>
                            <%= image_tag current_user.avatar.url, class: "rounded-circle p-3" %>
                        <% else %>
                            <%= image_tag "no_avatar.jpg", class: "rounded-circle p-3" %>
                        <% end %>
                        <h5 class="card-title"><strong><%= current_user.name %></strong></h5>
                        <a href="#" class="btn btn-primary">View profile</a>
                        <a href="#" class="btn btn-primary">Edit profile</a>
                    </div>
                </div>
                <div class="relation-number" style="width: 18rem;">
                    <%= current_user.following.count %> Following
                    <%= current_user.followers.count %> Followers
                </div>
                <br>
                <div class="card" style="width: 18rem;">
                    <%# <%= Method of get number of finished lesson number %>
                    words learned
                </div>
            </div>
            <div class="col-8">
                <div class="jumbotron">
                    <h4>ACTIVITY FEED</h4>
                </div>
            </div>
        </div>
    </div>
</div>